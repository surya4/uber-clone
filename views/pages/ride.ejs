<html lang="en">
    <% include ../partials/header %>
    <body>
            <!-- <p class="centre-main col-md-12">hello</p> -->
      <% include ../partials/nav %>
      <p class="centre-main col-md-12">hello</p>
      <div class="container">
          <p>a</p>
        <div class="row">
                <p>b</p>
          <!-- <div class="panel">
            <div class="panel-body">
              <% if (messages.success) {%>
              <div class="alert alert-success">
                <% messages.success.forEach(function(msg){ %>
                <p>
                  <%= msg %>
                </p>
                <% });%>
              </div>
              <% }%>
              <% if (messages.error) {%>
              <div class="alert alert-danger">
                <% messages.error.forEach(function(msg){ %>
                <p>
                  <%= msg %>
                </p>
                <% });%>
              </div>
              <% }%>
              <% if (messages.info) {%>
              <div class="alert alert-info">
                <% messages.info.forEach(function(msg){ %>
                <p>
                  <%= msg %>
                </p>
                <% });%>
              </div>
            </div>
          </div> -->

          <!-- <div class="centre-main col-md-12"></div> -->

        </div>
        <div class="centre-main col-md-12">abra</div>
      </div>
      <div class="centre-main col-md-12">dabra</div>
      <% }%>
      <% include ../partials/footer %>
      <% include ../partials/scripts %>
    </body>
  </html>